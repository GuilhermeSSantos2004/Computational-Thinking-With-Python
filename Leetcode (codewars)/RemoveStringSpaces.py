def no_space(x):
    frase_sem_espacos = ''.join(x.split())
    return frase_sem_espacos

def no_space(x):
    return x.replace(' ' ,'')
    
# Divide a frase em palavras usando split() 
# une as palavras sem espaços usando join()
# replace (substitui isso, por aquilo)
